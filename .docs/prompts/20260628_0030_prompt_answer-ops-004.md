# Antworten + Anmerkungen zu Modul 004 ops (Stand-Report 2026-06-28)

> Spec geprüft (alle 7 Dateien). Urteil: konsistent, vollständig, entspricht dem Konzept.
> Engine-Architektur, Bildverarbeitung (Claude Vision, Mistral=FiBu-only), Kern-Umzug,
> git-Chronik — alles sauber. Folgendes noch erledigen:

## Antworten auf die 4 Rückfragen

1. **Version:** Bleibt im Cycle **`in_arbeit`** (0.4.1). Kein Meilenstein — offene Punkte
   (Forderung/Kaution, KI-Schwellen) zuerst. Nicht einfrieren.

2. **Schaden-Kostenträger:** NICHT hart `mieter`. **Pro Richtung automatisch** (Default,
   Override möglich):
   - Auszug-Übergabe → `mieter` (Verschulden → Kaution).
   - Einzug-Übergabe → `vermieter` (Bestandsschaden, kein Mieterverschulden).
   - Hinweis: dein ENUM `(vermieter/mieter/versicherung/weg)` passt — `vermieter` ist hier
     das richtige Wort (nicht „eigentuemer"). Bleib dabei.

3. **Idempotenz Schaden-Übernahme:** **Ja, bauen.** Übernommene Vorschläge in `ki_analyse`
   markieren (`uebernommen: true` + Folge-Vorgang-ID); Reload darf nicht doppelt anlegen.
   Passt zum Kern-Prinzip Dubletten-Block.

4. **README-Dublette entfernt:** **Korrekt so.** War der verworfene Chat-Grobentwurf, kein
   echter Stand. Entfernen richtig, nicht „abgelöst markieren".

## Zusätzlich zu prüfen / entscheiden

5. **Kern-Verweis prüfen (wichtig):** In 004 steht „Vorgänge ziehen aus 0001 → 004, im Kern
   nur Verweis + Verknüpfungspunkte". Bitte sicherstellen, dass die **Kern-Spec `001_erp_*`**
   tatsächlich auf diesen Verweis reduziert ist (kein doppelter Vorgangs-Inhalt mehr in 0001).
   Wenn 0001 noch alten Vorgangs-Text trägt → auf „Vorgänge → 004" kürzen, mit Historie.

6. **dnd-kit vs. native DnD — Konsistenz mit CRM (0003):** ops-Plantafel nutzt native HTML5-DnD
   („wie CRM-Board"). Die CRM-Spec 0003 hatte aber **dnd-kit** festgelegt. Bitte angleichen:
   entweder beide Boards native, oder beide dnd-kit. Aktuell driften 0003 (Spec) und die reale
   Umsetzung auseinander — eine Variante wählen, beide Specs konsistent ziehen.

7. **Typ-Icons (Kosmetik):** Emoji (⚠🔧🧹🔑🛠✉) als Typ-Icons weichen vom Kern-Icon-Set
   (Lucide/Tabler) ab. Erwägen, auf das Kern-Set umzustellen — einheitlicher in einer
   professionellen ERP-UI. Kein Blocker.

## Offene Punkte aus dem Report (bestätigt, kein Defekt)

- Forderung/Kaution-Verknüpfung beim übernommenen Schaden automatisch setzen (hängt an
  Kautionsabrechnung Kern) — wenn Kern soweit ist.
- OP-6: alte 002-Tabellen (`ma_profile`/`einsaetze`/…) droppen, sobald nichts mehr
  referenziert — eigener idempotenter Migrationsschritt.
- Modellgüte-Vorbehalt bleibt: Auto-Confidence-Schwellen erst nach Test an 20–30 echten
  Vorher/Nachher-Paaren scharfstellen; bis dahin kritische Felder generell „nie auto".
