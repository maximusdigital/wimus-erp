# Auftrag: Migration 029 (Org Phase B0) ANWENDEN — Backup steht (2026-06-29 20:30 MESZ)

Freigabe von Max liegt vor: **DB-Backup verifiziert** (custom-Dump `wimus_pre_b0_20260701_2020.dump`,
alle Kern-Tabellen als TABLE DATA enthalten, PGDMP-Format bestätigt). firmen.typ-Werte werden über
die UI gepflegt (NICHT per Migration) → 029 setzt nur die CHECK-Struktur, keine Werte. Damit sind
beide Freigabe-Voraussetzungen erfüllt. 029 darf angewandt werden.

## Vorstufe (PFLICHT vor dem Anwenden) — Backup aus Container-Root wegsichern
Der Dump liegt aktuell im Container-Root (`/wimus_pre_b0_20260701_2020.dump`) — das ist der
beschreibbare Container-Layer, NICHT persistent (Redeploy = weg). Vor dem Anwenden MUSS Max den
Dump wegsichern. **Das kann CC NICHT selbst (kein Host-/Container-Shell-Zugriff über /pg/query).**
→ CC: im Report/Chat Max daran erinnern, den Dump aufs persistente PGDATA-Volume ODER die Hetzner
Storage Box zu kopieren, BEVOR 029 läuft. Wenn Max bestätigt „Dump gesichert" → weiter. Sonst warten.
(Falls Max den cp schon gemacht hat: kurz bestätigen lassen, dann fortfahren.)

## Anwenden — Migration 029
Datei liegt fertig + committet: `supabase/migrations/029_org_phase_b0.sql` (Commit ea58f99).
Inhalt (DDL-only, idempotent, fasst KEINE Bestandsdaten an):
1. `alter table wimus.projekte drop column if exists marke;`
2. `alter table wimus.firmen drop constraint if exists firmen_typ_check;`
   `alter table wimus.firmen add constraint firmen_typ_check
      check (typ is null or typ in ('privat','Einzelunternehmung','GbR','GmbH'));`

**Der im Header geparkte per-Firma-UPDATE-Block wird NICHT gebraucht** — Max pflegt die typ-Werte
über die UI. Also NUR die beiden DDL-Schritte anwenden, sonst nichts.

### Ablauf (Guardrail einhalten)
1. **Re-Check vor Anwendung** (read-only, sicherheitshalber):
   `SELECT count(*) total, count(marke) mit_marke FROM wimus.projekte;` → erwartet total 7, mit_marke 0.
   Falls mit_marke > 0: STOPP, NICHT droppen, Report.
2. Exakte SQL (die 3 ALTER-Statements) im Report zeigen.
3. Über `POST https://supa.m81s.de/pg/query` (Service-Role) anwenden.
4. **Verifizieren** (read-only):
   - `SELECT count(*) FROM information_schema.columns WHERE table_schema='wimus'
      AND table_name='projekte' AND column_name='marke';` → 0
   - `SELECT conname, pg_get_constraintdef(oid) FROM pg_constraint
      WHERE conrelid='wimus.firmen'::regclass AND conname='firmen_typ_check';`
      → neue Liste (privat/Einzelunternehmung/GbR/GmbH)

## NICHT tun
- KEINE firmen.typ-Werte setzen (kommt über UI).
- KEINE Bestandsdaten (objekte/einheiten/…) anfassen — das ist B1/B2, separate Aufträge.
- Migration NICHT verändern (029 ist fertig + committet). Nur anwenden + verifizieren.
- Specs/Backlog NICHT ändern (Konzept-Claude-Hoheit).
- 004 (alte typ-CHECK im Kommentar) NICHT anfassen — Konzept-Claude entscheidet separat, ob nachgezogen.

## Report
`.docs/reports/JJJJMMTT_UHRZEIT_report_029-angewandt.md` (4 Abschnitte):
- Backup-Wegsicherung bestätigt (ja/nein, wohin)?
- Re-Check-Ergebnis (marke leer?), exakte angewandte SQL, Verifikations-Ergebnisse.
- Status: 029 angewandt ✅ / gestoppt + Grund.
- Rückfrage/nächster Schritt: B1 (No-Op, gesellschaften leer) + B2 (objekte.firma_id +
  einheiten.projekt_id Backfill, ~10 Objekte, mit Max-Gegenprüfung der Einheiten) als Folgeaufträge.

## Leitplanken
- Ohne Backup-Wegsicherungs-Bestätigung von Max: NICHT anwenden.
- Bei Unsicherheit/Re-Check-Auffälligkeit: STOPP + parken.
- Kein force-push. Idempotent. Commit-Status der Migration bleibt; nur Report neu.
