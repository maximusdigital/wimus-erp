# Report: Ausgehender WhatsApp-Sendeweg (Modul 007) (2026-06-29 10:20 MESZ)

Auftrag (Chat): „check den rest" (Deploy geparkt). „Rest" = der symmetrische Ausgangsweg, nachdem
der GreenAPI-apiToken verschlüsselt abgelegt + `KOM_SECRET_KEY` gesetzt ist. Commit `b9bc589`.
394 Tests grün, build grün. Keine Migration nötig.

## 1. Gebaut
- **`persistiereAusgehend`** (`lib/kommunikation/inbox.ts`): schreibt `kom_nachrichten`
  (`richtung='ausgehend'`, Status/`extern_id`/`fehler_text`/`gesendet_am` aus dem Sende-Ergebnis),
  Bezüge (`kom_nachricht_bezug`) und bei Erfolg+Kontakt die Historie-Aktivität
  **`nachricht_gesendet`** (Modul 009, mit Einheit/Objekt-Hierarchie). Spiegelbild zu
  `persistiereEingehend`. Helper `mieterEinheitObjekt` extrahiert (wird auch von `findeKontakt`
  genutzt — keine Doppelung).
- **`lib/kommunikation/send.ts` → `sendeWhatsapp()`**: Orchestrator — WA-Instanz laden,
  GreenAPI-Token **entschlüsseln** (`crypto.ts` / `KOM_SECRET_KEY`), Adapter bauen, senden,
  Ergebnis persistieren (auch Fehler → Retry/Diagnose). Adapter wirft nie; Persistenzfehler
  verschluckt das Sende-Ergebnis nicht.
- **4 Unit-Tests** (Instanz inaktiv/unbekannt · kein/ungültiger Token · Erfolg · Sende-Fehler).

## 2. Abweichungen / Realität
- **Kein Consumer/Trigger live verdrahtet.** `sendeWhatsapp()` ist die Service-API; ein Aufrufer
  (Reply-UI in der Inbox / Autoreply-Engine / Vorgangs-Antwort) existiert noch nicht. Bewusst:
  Versand ist außenwirksam → erst mit konkretem Trigger + nach Deploy aktiv. **Es wurde keine echte
  WhatsApp gesendet.**
- Token wird je Sende-Vorgang frisch entschlüsselt (kein Klartext in DB/Logs).

## 3. Offen / Folge
- **Reply-UI / Autoreply als Consumer** von `sendeWhatsapp()` (nach Deploy). `autoreply.ts` +
  `signatur.ts` (007) sind dafür vorhanden und können andocken.
- **Deploy** (geparkt): erp.m81s.de ist aktuell unhealthy (Root 500, Webhook-Routen 404) — erst
  danach funktioniert Empfang UND Versand live. Decrypt im Server braucht `KOM_SECRET_KEY` auch in
  der **Deploy-Env** (nicht nur `.env.local`).
- Anhänge ausgehend (`sendFileByUrl`) — Stufe 2.

## 4. Rückfragen
- Soll der **Consumer** als Nächstes gebaut werden (z.B. einfache Reply-Funktion in der Inbox-/
  Vorgangs-Ansicht), oder erst nach Deploy + Live-Test des Empfangs?
- `KOM_SECRET_KEY` in der **Deploy-Umgebung** (Coolify-Env) ebenfalls setzen — sonst schlägt das
  Entschlüsseln serverseitig fehl.
