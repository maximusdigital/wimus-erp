# Nachtrag: Migration 031 (ist_admin) ANGEWANDT — 2026-07-01 23:35 MESZ

Ergänzung zu `20260701_2320_report_admin-stufe0.md`. Freigabe von Max erteilt („ja").

## Angewandt + verifiziert

- **`supabase/migrations/031_admin_stufe0.sql`** über `db-apply.mjs 031` (`/pg/query`) eingespielt → HTTP 200.
- Verifikation (read-only):
  - `pg_proc`: `wimus.ist_admin` existiert, `prosecdef=true` (security definer). ✅
  - `SELECT wimus.ist_admin()` als service_role → `false` (auth.uid() null) — **läuft fehlerfrei**. ✅
  - `has_function_privilege('authenticated', 'wimus.ist_admin()', 'execute')` → `true`. ✅

## Status

- Admin-Bereich Stufe 0 ist damit **live-fähig**: `istAdmin()` (rpc) trifft die reale Funktion; der
  fail-closed-Zustand (Report 2320, Abschnitt 3) ist aufgehoben.
- Rollenlogik (superadmin|mandant_admin, zeitliche Gültigkeit) greift, sobald ein eingeloggter Nutzer
  eine entsprechende `benutzer_rollen`-Zeile hat — die tatsächliche true/false-Auswertung je Rolle ist
  erst mit echtem Auth-Kontext (eingeloggter Admin) beobachtbar (Service-Role hat keinen).
- Offen unverändert: Benutzer-**Anlegen** (Auth-Admin-Weg klären) + Stufe 1 (Rechte-Matrix/Scope).
