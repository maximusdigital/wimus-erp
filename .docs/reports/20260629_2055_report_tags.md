# Report: Tags — Kern-Querschnitt gebaut + angewandt (001) — 2026-06-29 20:55 MESZ

Auftrag: `.docs/prompts/20260629_1845_prompt_tags.md`. Voller Zyklus inkl. Anwendung.
Commit `55be904` (Migration ANGEWANDT + verifiziert). Tests 407 grün, Build grün.
Review-Subagent: **FREIGABE**. Über `/pg/query` eingespielt (Guardrail: SQL vorab gezeigt + Freigabe „ja").

## 1) Gebaut (mit echten Tabellen/Feldern)

`supabase/migrations/030_tags.sql` (nächste freie Nr.: 029 belegt → 030), idempotent, `search_path wimus`.
Zwei neue Tabellen im Kern (Modul 001), Muster nach `custom_field_werte`/`custom_field_value_option` (027):

- **`wimus.tags`**: `id`, `mandant_id` FK→mandanten ON DELETE CASCADE, `label` TEXT, `farbe` TEXT NULL,
  `aktiv` BOOL, `created_at`/`updated_at`. **UNIQUE `ux_tags_mandant_label (mandant_id, lower(label))`**
  (case-insensitive eindeutig je Mandant). Touch-Trigger `trg_tags_touch`.
- **`wimus.tag_zuordnung`**: `id`, `tag_id` FK→tags ON DELETE CASCADE, `bezug_typ` TEXT (FREI, KEINE
  Whitelist), `bezug_id` UUID (polymorph, KEIN FK), `created_at` (bewusst KEIN updated_at).
  **UNIQUE `uq_tag_zuordnung (tag_id, bezug_typ, bezug_id)`** + Index `ix_tag_zuordnung_bezug (bezug_typ, bezug_id)`.
- **RLS mandant_isolation** (FOR ALL TO authenticated): `tags` über eigene `mandant_id IN (SELECT wimus.user_mandanten())`;
  `tag_zuordnung` über das Eltern-Tag (EXISTS auf `tags.mandant_id`). **GRANTS**: authenticated
  (SELECT/INSERT/UPDATE/DELETE) + service_role (ALL). **KEIN Seed**.

## 2) Anwenden + Verifikation (live)

Eingespielt über `db-apply.mjs 030` (BEGIN…COMMIT, idempotent). Verifiziert (read-only + Smoke):
- `to_regclass`: beide Tabellen existieren.
- RLS: `relrowsecurity=true` auf beiden; Policy `mandant_isolation` (cmd ALL) auf beiden.
- Indizes: `ux_tags_mandant_label`, `uq_tag_zuordnung`, `ix_tag_zuordnung_bezug` (+ PKs) vorhanden.
- **Smoke (service_role):** Test-Tag + Zuordnung angelegt (bezug_typ='objekt') → **case-insensitive
  UNIQUE greift** (lowercase-Dublette abgelehnt) → `DELETE tag` **CASCADE** räumte die Zuordnung →
  0 Reste. Alle Smoke-Objekte wieder entfernt.

## 3) Abweichungen (inkl. Review-Findings)

- **Kein lib-Helper gebaut** (bewusst): Das Projekt hat keine DB-Integrations-Tests (Vitest = gemockt,
  keine Live-DB), ein `lib/tags/`-Helper hätte weder Testabdeckung gegen echtes Verhalten noch einen
  Konsumenten (UI ist NICHT Teil dieses Auftrags). Daher als **Vorschlag** offen (s. Abschnitt 4).
- Review-Findings (beide KLEIN, kein Handlungsbedarf):
  - Index heißt `ix_tag_zuordnung_bezug` (Prompt-Vorschlag: `ix_tagzuordnung_bezug`) — rein kosmetisch,
    Spec fixiert keinen Index-Namen.
  - `anon`-USAGE-Grant nicht gesetzt — für eine RLS-geschützte authenticated-Tabelle nicht nötig/erwünscht
    (Schema-USAGE für anon existiert bereits aus 006); bewusste, korrekte Auslassung.

## 4) Offen / Vorschlag

- **lib-Helper `lib/tags/` (Vorschlag, nicht gebaut):** schlanker server-only Helfer nach
  `lib/felder/value.ts`-Muster — `createTag(findOrCreate über lower(label))`, `listTags`,
  `assignTag`(Upsert onConflict), `unassignTag`, `getTagsFor(bezugTyp,bezugId)`. Kommt sinnvoll
  zusammen mit der Tag-UI (Chips/Filter) — die überschneidet sich mit #21-B5 (Tag-Filter/-Summen),
  dort nicht doppeln.
- **UI** (Tag-Chips an Detailseiten, Mehrfach-Filter, Tag-Summen im Finanzteil) = separater Auftrag.
- Verwaisungs-Cleanup: `bezug_id` ist FK-frei (polymorph) → beim Löschen eines Ziel-Datensatzes
  bleiben Zuordnungen zurück; per Spec bewusst über späteren Cleanup-Job toleriert (nicht in diesem Bau).

## 5) Rückfragen
- Keine blockierenden. Migration additiv, angewandt, verifiziert. Möchte Max den lib-Helper vorgezogen
  (vor der UI) oder gebündelt mit der Tag-UI?
